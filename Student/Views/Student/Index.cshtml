@model IEnumerable<StudentSystem_Model.Models.Student>;

@{
    ViewData["Title"] = "Index";
}

<!DOCTYPE HTML>
<html>
<head>
    <title></title>
</head>
<body>
    <div class="d-flex justify-content-between align-items-center mb-0 p-3 rounded-top bg-body-secondary">
        <h2>Students List</h2>
        <a class="btn btn-success" asp-action="Upsert">
            Create New Student
            <span class="badge text-bg-warning mx-1">@Model.Count()</span>
        </a>
    </div>
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped text-center w-100">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Student Name</th>
                    <th>Email</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var student in Model)
                {
                    <tr>
                        <td>@student.StudentId</td>
                        <td>@student.FullName</td>
                        <td>@student.Email</td>
                        <td class="d-flex justify-content-center gap-5">
                            <a class="btn btn-primary" asp-action="Upsert" asp-route-Id="@student.StudentId">Update</a>
                            <a class="btn btn-secondary" asp-controller="StudentCourse" asp-action="Enrollment" asp-route-StudentId="@student.StudentId">Enroll Course</a>
                            <a class="btn btn-danger" asp-action="Delete" asp-route-Id="@student.StudentId">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h2>No Students Available</h2>
    }
    <script src="~/lib/bootstrap/dist/js/bootstrap.js"></script>
</body>
</html>
